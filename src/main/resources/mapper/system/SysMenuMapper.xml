<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.devlovecode.aiperm.modules.system.mapper.SysMenuMapper">

    <resultMap id="BaseResultMap" type="com.devlovecode.aiperm.modules.system.entity.SysMenu">
        <id column="id" property="id"/>
        <result column="menu_name" property="menuName"/>
        <result column="parent_id" property="parentId"/>
        <result column="menu_type" property="menuType"/>
        <result column="sort" property="sort"/>
        <result column="path" property="path"/>
        <result column="component" property="component"/>
        <result column="icon" property="icon"/>
        <result column="is_external" property="isExternal"/>
        <result column="is_cache" property="isCache"/>
        <result column="visible" property="visible"/>
        <result column="status" property="status"/>
        <result column="permission" property="permission"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
        <result column="deleted" property="deleted"/>
        <result column="version" property="version"/>
    </resultMap>

    <select id="selectMenuTree" resultMap="BaseResultMap">
        SELECT * FROM sys_menu
        WHERE deleted = 0
          AND status = 0
        ORDER BY parent_id ASC, sort ASC
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT DISTINCT m.*
        FROM sys_menu m
                 INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
                 INNER JOIN sys_user_role ur ON rm.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND m.deleted = 0
          AND m.status = 0
        ORDER BY m.parent_id ASC, m.sort ASC
    </select>

    <select id="selectByRoleId" resultMap="BaseResultMap">
        SELECT m.*
        FROM sys_menu m
                 INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
        WHERE rm.role_id = #{roleId}
          AND m.deleted = 0
        ORDER BY m.parent_id ASC, m.sort ASC
    </select>

    <select id="selectByParentId" resultMap="BaseResultMap">
        SELECT * FROM sys_menu
        WHERE parent_id = #{parentId}
          AND deleted = 0
        ORDER BY sort ASC
    </select>

    <select id="countByParentId" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM sys_menu
        WHERE parent_id = #{parentId}
          AND deleted = 0
    </select>

</mapper>
