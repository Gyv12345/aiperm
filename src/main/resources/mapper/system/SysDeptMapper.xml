<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.devlovecode.aiperm.modules.system.mapper.SysDeptMapper">

    <resultMap id="BaseResultMap" type="com.devlovecode.aiperm.modules.system.entity.SysDept">
        <id column="id" property="id"/>
        <result column="dept_name" property="deptName"/>
        <result column="parent_id" property="parentId"/>
        <result column="sort" property="sort"/>
        <result column="leader" property="leader"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
        <result column="deleted" property="deleted"/>
        <result column="version" property="version"/>
    </resultMap>

    <select id="selectDeptTree" resultMap="BaseResultMap">
        SELECT * FROM sys_dept
        WHERE deleted = 0
          AND status = 0
        ORDER BY parent_id ASC, sort ASC
    </select>

    <select id="selectByParentId" resultMap="BaseResultMap">
        SELECT * FROM sys_dept
        WHERE parent_id = #{parentId}
          AND deleted = 0
        ORDER BY sort ASC
    </select>

    <select id="countByParentId" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM sys_dept
        WHERE parent_id = #{parentId}
          AND deleted = 0
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT d.*
        FROM sys_dept d
                 INNER JOIN sys_user u ON d.id = u.dept_id
        WHERE u.id = #{userId}
          AND d.deleted = 0
        LIMIT 1
    </select>

</mapper>
